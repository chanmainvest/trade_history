services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - "G:/My Drive/Investment:/app/Statements:ro"
    environment:
      - STATEMENTS_DIR=/app/Statements
      - DB_PATH=/app/data
    restart: unless-stopped
